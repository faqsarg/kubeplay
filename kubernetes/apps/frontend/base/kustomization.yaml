apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# BASE = the environment-agnostic definition of the frontend app (static nginx,
# no ConfigMap/HPA). No namespace, no image tag — those are per-environment
# decisions that live in the overlays.
resources:
- deployment.yaml
- service.yaml
