# Copy to .env and adjust. .env is gitignored — never commit real secrets.
POSTGRES_USER=kubeplay
POSTGRES_PASSWORD=changeme
POSTGRES_DB=kubeplay

# Connection string used by the backend (must match the values above).
DATABASE_URL=postgres://kubeplay:changeme@localhost:5432/kubeplay?sslmode=disable
